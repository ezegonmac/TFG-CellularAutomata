"""
Generate datasets with different rules strategies.

Two types of datasets:
- default: all attributes stored (used for data analysis)
- density: only rules parameters and density evolution stored (used for prediction)

Modules exported by this package:

- `CAFactory`: Implements a factory for automating the creation of CA objects with different rule strategy.
"""
