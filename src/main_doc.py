from constants import *
from figures.doc import *
from learning.misc.doc import *

if __name__ == '__main__':
    
    # generate_states_figure()
    # generate_grid_figure()
    # generate_moore_figure()
    # generate_interval_figure()

# TABLES #

# - BTST

    # generate_scores_table(dataset=DATASET8_DENSITY)
    # generate_score_by_iteration_table(dataset=DATASET8_DENSITY)
    # generate_scores_table(dataset=DATASET3_DENSITY)
    # generate_score_by_iteration_table(dataset=DATASET3_DENSITY)
    # generate_scores_table(dataset=DATASET9_DENSITY)
    # generate_score_by_iteration_table(dataset=DATASET9_DENSITY)
    # generate_scores_table(dataset=DATASET10_DENSITY)
    # generate_score_by_iteration_table(dataset=DATASET10_DENSITY)

# - BISI

    # generate_scores_table(dataset=DATASET11_DENSITY)
    # generate_score_by_iteration_table(dataset=DATASET11_DENSITY)
    # generate_scores_table(dataset=DATASET12_DENSITY)
    # generate_score_by_iteration_table(dataset=DATASET12_DENSITY)
    # generate_scores_table(dataset=DATASET13_DENSITY)

# - BS

    # generate_scores_table(dataset=DATASET14_DENSITY)
    # generate_score_by_iteration_table(dataset=DATASET14_DENSITY)
    # generate_scores_table(dataset=DATASET15_DENSITY)
    # generate_score_by_iteration_table(dataset=DATASET15_DENSITY)
    # generate_scores_table(dataset=DATASET13_DENSITY)


    # v1 = 1.697
    # v2 = 3.788
    
    # percent_increase = (v2 - v1) / v1 * 100
    # print(percent_increase)
