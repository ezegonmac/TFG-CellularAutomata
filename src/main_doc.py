from constants import *
from figures.doc import *
from learning.doc import *

if __name__ == '__main__':
    
    # generate_states_figure()
    # generate_grid_figure()
    # generate_moore_figure()
    # generate_interval_figure()

    # generate_score_by_iteration_table(dataset=DATASET8_DENSITY)
    # generate_score_by_iteration_table(dataset=DATASET3_DENSITY)
    generate_score_by_iteration_table(dataset=DATASET9_DENSITY)
