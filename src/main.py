from constants import *
from datasets.datasets import *
from statistics.densities import *
from statistics.states import *
from figures.doc import *

if __name__ == '__main__':

    # generate_states_figure()
    # generate_grid_figure()
    # generate_moore_figure()
    # generate_interval_figure()

    # generate_dataset1()
    # generate_dataset2()
    # generate_dataset3()
    # generate_dataset4()
    # generate_dataset5()
    # generate_dataset6()
    # generate_dataset7()

    # create_state_plot(DATASET1, individual=0, iteration=0, show=False)
    # create_state_plot(DATASET1, individual=0, iteration=1, show=False)
    # create_state_plot(DATASET1, individual=0, iteration=2, show=False)

    # create_state_plot(DATASET1, individual=20, iteration=0, show=False)
    # create_state_plot(DATASET1, individual=20, iteration=1, show=False)
    # create_state_plot(DATASET1, individual=20, iteration=2, show=False)

    # create_state_plot(DATASET3, individual=0, iteration=0, show=False)
    # create_state_plot(DATASET3, individual=0, iteration=1, show=False)
    # create_state_plot(DATASET3, individual=0, iteration=2, show=False)
    # create_state_plot(DATASET3, individual=0, iteration=3, show=False)

    # create_state_plot(DATASET3, individual=50, iteration=0, show=False)
    # create_state_plot(DATASET3, individual=50, iteration=1, show=False)
    # create_state_plot(DATASET3, individual=50, iteration=2, show=False)
    # create_state_plot(DATASET3, individual=50, iteration=3, show=False)

    # create_state_plot(DATASET3, individual=120, iteration=0, show=False)
    # create_state_plot(DATASET3, individual=120, iteration=1, show=False)
    # create_state_plot(DATASET3, individual=120, iteration=2, show=False)
    # create_state_plot(DATASET3, individual=120, iteration=3, show=False)
